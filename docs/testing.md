# Testing

## Running

```bash
pytest tests/ -v        # free suite — no API cost, paid tests are excluded by default
pytest -m paid -v       # live tests against the real OpenAI API (fractions of a cent)
```

The default exclusion comes from `pytest.ini` (`addopts = -m "not paid"`). The
PostgreSQL-backed tests skip themselves when no database is reachable, so the free
suite is green everywhere.

## Database isolation

DB-backed tests never touch the operator's corpus. The `db_dsn` fixture (`tests/conftest.py`)
creates a throwaway `finiex_test` schema, applies the **real** `migrations/` into it, and returns
a DSN carrying `search_path=finiex_test,public` — isolation is the DSN's job, so no production
code knows a test is running. Tests therefore use the canonical table names (`articles`,
`cost_log`, …) against the exact schema the repo defines: a **broken** migration fails the suite
instead of hiding behind hand-written test DDL. `clean_db` adds a truncate for a per-test blank
slate. See [development/migrations.md](development/migrations.md).

`clean_db` reads its target list from `pg_tables` for the test schema and truncates each name
**schema-qualified**, minus the `schema_migrations` ledger. Two failure modes disappear with the
literal list it replaced: a name the migrations do not create in `finiex_test` used to resolve
through the `search_path` fallback to `public.<name>` — the operator's real table, truncated by a
test that then passes — and a table added by a later migration but forgotten in the list used to
leak rows between tests, which is the order-dependent flake nobody finds for months.

Note what this deliberately cannot cover: the fixture builds its schema from scratch every run, so
**checksum drift never appears here** — it only exists against a database that already applied an
older version of a file. Drift is caught by `migrate_cli --status` and the boot guard, on a real
database, not by the suite.

## Cost fencing (`paid` marker)

Tests that spend real API budget carry the `paid` marker and live in clearly-named
`*_live.py` files. They are excluded from every default run and from CI; they exist to
validate what mocks cannot: authentication, model behavior, semantic quality. Run them
deliberately via `pytest -m paid` or the 💸 launch entry in `.vscode/launch.json`.

The API contract tests build the app with `create_app(attach_runners=False)` — pinned to
scaffold-mock mode. A real runner behind `/run` makes paid calls, so the free suite must
never attach one just because `DATABASE_URL`/`OPENAI_API_KEY` are set in the environment.

## Layout

The suite mirrors the package: **a test lives where its subject lives.** `tests/rag/` covers
`finiexragengine/core/rag/`, `tests/api/` covers `api/`, `tests/observability/reports/` covers
`core/observability/reports/` — so finding the tests for a unit is the same navigation as finding
the unit, and a crowded flat directory can no longer hide which corner of the engine is untested.

```
tests/
  conftest.py                 shared fixtures (client, db_dsn, clean_db)
  fixtures/signals/           tracked sample data — the consumer's contract sample
  api/  cli/  configuration/  the surfaces and the config layer
  sources/  rag/  llm/        the stages, in pipeline order
  pipeline/  outcome/         orchestration + the produced envelopes
  observability/              metrics units, stores, watchdogs
    reports/                  the console/report surfaces
  ui/  alerts/  triggers/  schema/  utils/
  contracts/                  repo-wide convention guards (typing, closed vocabularies)
  generator/                  the consumer sample generators under experiments/
```

Two folders are not package mirrors, deliberately. `contracts/` holds the guards that are about
the *codebase* rather than about a unit — the typing sweep over `finiexragengine/` and the
closed-vocabulary boundary; `generator/` holds the tests for the sample generators in
`experiments/`, which are tools rather than engine code. Everything else names a package domain.

A new test goes into the folder its subject already occupies. If none fits, a new folder is created
for the category rather than dropping the file at the root — the flat root is what this layout
replaced.

No `__init__.py` anywhere (the project convention): pytest's `prepend` import mode imports each
file by its basename, so **basenames stay unique across the whole tree** — two `test_report.py`
in different folders would collide at collection.

## Suites

| File | Covers | Needs |
|---|---|---|
| `cli/test_server_cli.py` | the two things the launcher decides (ISSUE_98): the default bind is **loopback** — the engine is reached through the TLS proxy, never directly — and a `ConfigurationError` from the app factory prints as a message with exit 1 instead of a uvicorn traceback, because a problem a human fixes in five seconds must not read like a crash | — |
| `configuration/test_setting_resolver.py` | the shared precedence rule for settings that may arrive from two places (ISSUE_98): the environment wins, `user_configs` fills in, a blank variable counts as absent, the parser runs only on the environment branch (a config value is already typed), and `printable` decides whether the boot line may echo the value — a certificate path may, a credential never | — |
| `api/test_api_auth.py` | connection security (ISSUE_98): every protected route rejects an unauthenticated call · **a route registered inside the test inherits the dependency** — the router-level guarantee asserted rather than described · valid vs wrong token · the credential reaches no log line and no response body (not even an 8-char prefix) · `/health` answers without one · `POST /run` is absent from the OpenAPI schema *and* answers 404 when disabled · boot refuses with auth on and no tokens · the registry keeps digests not tokens and rejects a malformed `FINIEX_API_TOKENS` · the token bucket, its per-client key behind a proxy, and failure throttling · **the exemptions in both directions**: `health_public: false` actually moves `/health` behind the token (it did not until 2026-08-24 — it only dropped the rate limit), `/v1/build` is public by default and closable the same way, and its payload is sampled once per process rather than per request; plus the ISSUE_104 report surface rejecting an unauthenticated call on the real app | — |
| `api/test_api_health.py` | API contract: health, pipeline listing, run envelope (mock mode) | — |
| `sources/test_article_normalizer.py` | text normalisation (ISSUE_112): one **exact-output** fixture per carrier class — HTML markup, entities, entity-encoded tags, zero-width/BOM, bidi overrides, `<script>`/`<style>` **bodies**, NBSP whitespace, NFC. Exact strings, never ratios: the first attempt at this shipped a character class that deleted almost all text while still producing a plausible token count. Plus idempotence; the prose guarantee stated testably (the **non-whitespace token sequence** is unchanged for carrier-free input — not byte identity, because collapsing `\xa0` is the intent); the length-bounded tag pattern proven to fail toward *keeping* text (an over-long tag survives, a stray `<` does not eat the field); `article_id` untouched; an unknown profile refused where it is configured; and the **regression fixture the issue was opened on** — the 6 cointelegraph CDN image filenames that alone flagged an article HIGH must not survive, while genuine prose keyword hits must | — |
| `sources/test_source_normalization.py` | the seam (ISSUE_112): a source implementing only `_fetch_articles` returns normalised, stamped articles without mentioning normalisation; the fetched bytes survive on the article; a source built without a normaliser still normalises (the safe behaviour is the default, so markup is never the reward for a forgotten argument); the factory passes the configured profile; and `fetch` is pinned as **not** abstract while `_fetch_articles` is — a subclass overriding `fetch` would opt its whole source type out silently | — |
| `sources/test_rss_source.py` | RSS → Article mapping, idempotent ids, conditional GET (304), poll floor, typed HTTP/transport failures (429/5xx/retry); fetch deadline (ISSUE_73): per-source override beats the set default, the handler actually stamps the request, and a blackholed host raises `UNREACHABLE` promptly instead of hanging — the 2026-08-01 incident as a test | — |
| `rag/test_openai_embedder.py` | batching, order preservation, dimension guard (mocked client); input fitting + rejection isolation (ISSUE_79): an over-long input is trimmed **before** it is sent, a provider-rejected item is isolated by bisection while every other item still embeds (the 2026-08-11 poison-article shape), two poison items are both isolated, a quota error is *not* bisected, and a token-count mismatch against the provider's own usage warns without failing the pass | — |
| `rag/test_token_budget.py` | exact token counting + truncation (ISSUE_79): a short text is untouched but still counted, an over-long one is cut to exactly the limit, the cut lands on a token boundary (re-fitting is a no-op), an unknown model raises at construction rather than mid-pass, the `encoding` override wins, and two equal-length texts of different density prove why a chars-per-token heuristic would not do | — |
| `rag/test_pgvector_store.py` | idempotent upsert, recency/similarity query, importance filter | PostgreSQL |
| `rag/test_retriever.py` | two-tier policy, top_k cap, near-dup collapse, tie-breaks, funnel counters (mocked) | — |
| `rag/test_symbol_query_map.py` | constellation alias + base-currency fallback | — |
| `rag/test_query_vector_cache.py` | cached query vectors, cache busting on config/model change | PostgreSQL |
| `pipeline/test_ingestor.py` | fetch → skip known ids → embed only new → upsert; per-source counts; health record + quarantine skip; budget suspend; embed-stage isolation (ISSUE_79): a provider-rejected article costs only itself — the pass still stores every other article and reports `ok` — plus truncation stamped onto the article row and counted on the pass; normalisation accounting (ISSUE_112): the pass counts what the normaliser removed, a clean feed grows no line, the count covers **everything fetched** rather than only new ids (it answers "how dirty is this feed", a property of the pull), and the embedder is proven to receive the normalised text — the half where the 36.7 % was actually spent; poll journaling (ISSUE_76): a **failed** fetch is recorded with the time it burned (verified to fail against the pre-#76 code), a successful one with its article count, skips are never journaled, and a pass without a journal is unchanged; ISSUE_107 added the pooled fetch phase — a pooled pass yields the identical result object to the sequential one (same polls, same order, same counters), the fetches really do overlap, the phase is timed once instead of once per feed so the perf report cannot claim a pass spent longer fetching than it existed, and a budget suspend rewinds the conditional-GET validators of every feed it fetched but never accounted for — without which those articles would answer 304 next pass and be lost | — |
| `observability/reports/test_coverage_report.py` | corpus coverage aggregation + console rendering | PostgreSQL |
| `llm/test_prompt_builder.py` | Jinja2 `.md` fill + versioning; front-matter metadata + body hash; the **dual context key** (v3 reads `{{ query }}`, v1/v2 still read `{{ symbol }}`) and a **hash pin on every shipped prompt** — a template edited in place would invalidate the `prompt_hash` recorded in every envelope it produced, so a change must land as a new version file (ISSUE_64 Phase 2) | — |
| `configuration/test_pipeline_prompt_config.py` | pipeline-declared `prompt` block (name + version) | — |
| `llm/test_sentiment_llm_output.py` | strict scored-subset schema (ranges, forbid extras); `breaking_reason` optional — present when the model wrote one, `None` on a quiet pass (ISSUE_64 Phase 2) | — |
| `llm/test_openai_provider.py` | structured call, error taxonomy mapping, cost capture (mocked) | — |
| `pipeline/test_symbol_evaluator.py` | retrieve → prompt → LLM → enrich; provenance, raw-output + funnel capture | — |
| `pipeline/test_pipeline_runner.py` | envelope invariants, taxonomy, guard degrade (ISSUE_35), metric capture, prompt fingerprint, persistence wiring | — |
| `pipeline/test_output_guard.py` | coherence rules (signal↔score dead zone, HOLD confidence cap, empty reasoning, provenance backstop), knob overrides, basis skip | — |
| `outcome/test_outcome_store.py` | save→get_latest roundtrip, newest-wins, raw-output column, error rows, the stream position stamped into the envelope JSON, per-stream sequencing, unsequenced legacy lines | PostgreSQL |
| `outcome/test_episode_backfill.py` | episode identity backfill (ISSUE_108) against a real database, because everything worth checking is a write: **parity with the live tracker** (the same series driven through `BreakingEpisodeTracker` and through the backfill yields identical ids — the property that justifies driving the tracker instead of a walk lifted out of the funnel report); a served id compared and never overwritten; a **planted disagreement aborting the write entirely**, including the stampable row beside it (an abort is not partial); a dry run writing nothing, asserted against the tables; **only the two episode keys moving** (the envelope minus them is unchanged, which is what `jsonb_set` on two paths buys over a whole-envelope rewrite); idempotency, where the second run's self-check also validates the first run's writes; **the prologue**, without which a continuation of an episode opened before the range mints an id from a clipped start; a fanned pair as one episode with `n_passes` counting passes and not symbols; and an `error` envelope outside the replay. Two of these caught a real defect: the rule holds the open-episode state and lives on the grouping, so a `plan()` before an `apply()` used to leave `breaking_episode_start` false on the opener while the id stayed right | PostgreSQL |
| `outcome/test_stream_sequencer.py` | gapless `seq` (a rolled-back pass returns its number), per-stream counters, concurrent minting, the `available_msc` clamp + its counters, epoch bumps on a counter behind the journal / a changed cluster fingerprint, and an epoch that never repeats after its own value was rewound | PostgreSQL |
| `outcome/test_episode_registry.py` | the persisted episode registry (ISSUE_65): the opening pass creates the row, a continuation advances `last_seen_at` + `n_passes` **without** rewriting the edge's frozen fields (signal/urgency/reaction), a replayed pass does not inflate the count, an out-of-order pass never moves `last_seen_at` backwards, eight concurrent writers of one key produce one row rather than a constraint error (the ISSUE_74 no-lock condition), and `OutcomeStore.save` commits the envelope and its episode row together | PostgreSQL |
| `observability/test_build_info.py` | build identity sampling: a repository yields a commit and a clean/dirty verdict, no git at all yields nulls instead of an exception (a container image must still boot), a failing git call is equally non-fatal, and `dirty` stays `None` rather than `False` when there is no repository — "clean" and "not determinable" are different answers | — |
| `utils/test_dataclass_json.py` | report serialization (ISSUE_104): a dataclass carries its fields **and** its public properties (`asdict` drops them, and `quarantined` is a now-relative verdict only the server can give), nested dataclasses/lists/dicts are walked, an aware datetime is normalised to **UTC and rendered with `Z`** (psycopg returns TIMESTAMPTZ in the session zone, so payloads carried server-local time — and a `+` offset cannot survive a query string) while a naive one is left alone, durations become seconds, sets serialize sorted so two identical reports match, a behaviour object publishes `report_values()` rather than its state, and an unrecognised object **raises** instead of leaking its `__dict__` the way a generic encoder would | — |
| `observability/reports/test_report_catalog.py` | the catalog (ISSUE_104): a loop over every entry builds **and** serializes it, so a report added later is covered the moment it is added; the listing declares each report's parameters and required ones; `required` is a subset of `params`; an unknown name raises `KeyError` for the transport to translate; and `coverage` is asserted **absent** — it embeds on a cache miss, so an entry for it would be a GET that spends | PostgreSQL |
| `api/test_report_scopes.py` | per-token grants (ISSUE_104): the catalog listing shows only what the caller can fetch, a report outside the scope answers **403 naming what the token may read** while an unknown name stays a 404 whatever the scope, `"*"` reads everything, an empty scope reads nothing but leaves `/v1/pipelines` untouched, an unknown consumer is denied rather than defaulted, and on the config side: `reports` is required, the old bare `"name": "token"` form is refused with the shape that replaces it, and an environment-supplied token (which has nowhere to put grants) resolves to `"*"`; plus the structural guarantee — a walk over every registered identity route asserting a token holding nothing is refused, so a router mounted without a declared surface fails here | PostgreSQL |
| `api/test_report_api.py` | the report transport (ISSUE_104): the catalog listing, one report answering with its data plus the window it actually used, an unknown name as 404 rather than 500, a missing required parameter as 422 naming it, an unparseable window as 422, a window beyond the ceiling clamped with `clamped: true`, one inside it untouched, and a report registered **inside the test** reachable through the generic route | PostgreSQL |
| `rag/test_corpus_guard.py` | corpus stamped with embedding model; mismatch refuses to boot | PostgreSQL |
| `configuration/test_source_set_registry.py` | source-set loading, duplicate ids, unknown reference, tracked configs | — |
| `pipeline/test_workers.py` | interval-trigger loop, pass resilience, supervisor build (fan variants); worker isolation (ISSUE_74): a worker blocked mid-pass does not stop another — the 2026-08-01 amplifier as a regression test — and a pass over its deadline is abandoned while the worker keeps ticking; the trigger reason reaches the pipeline and opens `last_detail`, the string the log line, the activity stream and /health share (ISSUE_87) | — |
| `pipeline/test_breaking_detector.py` | cluster-tier boundaries + keyword fast-path (word-boundary), all LLM-free ; ISSUE_106 added the path attribution — the trigger reaches the store on both routes, an overlap (a real burst that also carries a keyword) is credited to the **cluster** so the fast path's hit rate is not flattered, and a routine article records no path at all so NULL keeps meaning "not flagged"| — |
| `pipeline/test_breaking_bus.py` | per-pipeline `min_importance` wake filter, re-arm, cross-set isolation | — |
| `triggers/test_event_trigger.py` | eval clock: immediate + interval + breaking wake before interval, clean stop; the pass reason each tick reports (ISSUE_87) — `boot` for the immediate first run, `scheduled` per boundary, `breaking` when the wake wins the race, asserted at the source rather than inferred from a timestamp | — |
| `pipeline/test_detection_preflight.py` | detection-threshold reachability (ISSUE_106) written as the argument for the check existing: the exact state that filed the issue (4 active feeds against `high_cluster_size: 5`), the silent failure where the keyword gate sits above every active feed — a **proof**, unlike the cluster check, which is worded as the indicator it is — the gate distribution ISSUE_82 wanted and could not get, growth flipping the question at 20 feeds, the `+N more` cap on a parked catalogue, an all-disabled set that must not read healthy, and the boot line and report sharing one wording | — |
| `observability/reports/test_breaking_report.py` | reaction math (engine vs end-to-end), episode grouping, funnel render; the freshest-source anchor (ISSUE_81) — a stale context article does not drag the number, **and the store path agrees with the live path digit-for-digit on the same envelope** (the ISSUE_64 parity lesson, now asserted rather than assumed) — extended in ISSUE_82 to hold **across a restart**, the case that used to diverge silently because the live tracker started empty; and the ISSUE_64 Phase 2 guard — the story measure must keep clustering on `reasoning`, so two episodes whose `breaking_reason` lines share almost no vocabulary still count as **one** story ; ISSUE_106 added the two seams this file could not see: `build_breaking_report(stories=…)` must reach `_aggregate` (it did not — every test here drives `_aggregate` directly, so the pass-through above it was unexercised and the report grouped by the schema default while printing that default as the rule it applied), and the detection-reachability block, which renders its census whether or not a threshold is out of reach| — || `observability/reports/test_breaking_report.py` | reaction math (engine vs end-to-end), episode grouping, funnel render; the freshest-source anchor (ISSUE_81) — a stale context article does not drag the number, **and the store path agrees with the live path digit-for-digit on the same envelope** (the ISSUE_64 parity lesson, now asserted rather than assumed) — extended in ISSUE_82 to hold **across a restart**, the case that used to diverge silently because the live tracker started empty; and the ISSUE_64 Phase 2 guard — the story measure must keep clustering on `reasoning`, so two episodes whose `breaking_reason` lines share almost no vocabulary still count as **one** story ; ISSUE_106 added the two seams this file could not see: `build_breaking_report(stories=…)` must reach `_aggregate` (it did not — every test here drives `_aggregate` directly, so the pass-through above it was unexercised and the report grouped by the schema default while printing that default as the rule it applied), and the detection-reachability block, which renders its census whether or not a threshold is out of reach| —. Plus the one DB-backed case in that file: a database **without** an outcomes table returns a clean empty report rather than raising. `build_breaking_report` takes no `by_trigger` parameter but its early return referenced one — an idiom copied from `_aggregate`, which does — so binding the name further down made it function-local and the branch promising "a clean empty report, not a crash" raised `UnboundLocalError` on any fresh install. Found by an adversarial review of ISSUE_112's deploy checks, reproduced, then pinned |
| `pipeline/test_breaking_episode.py` | edge-triggered breaking episodes (ISSUE_11): first breaking = one episode, re-break within/after the gap, ongoing story counts once, reaction anchored+frozen at the start, estimated publish (published==fetched) excluded from e2e; the freshest-source anchor (ISSUE_81) — a 20h-old context article next to a 30s-old trigger must report 30s, source order does not change the result, a single source is unaffected; the hysteresis half (ISSUE_82) — a dip to the exit gate holds the story, a drop below it closes after the gap, opening uses the **recorded** verdict rather than today's threshold, and the measured 2026-08-17 XRPUSD series (14 real urgencies) must yield **one** episode where the old rule produced two; the ISSUE_82 finding-6 regression — a quiet USDJPY in the hold band must no longer hold a USDCAD episode open through its own base key; the seed replay — an episode whose last breaking pass predates the window start is still recovered, one already open at the first replayed envelope is marked as a lower bound, and a story that closed inside the window is not resurrected; the ISSUE_64 Phase 2 split — an episode prefers the dedicated `breaking_reason` for display while `reason` keeps carrying `reasoning`, and a pre-v3 envelope falls back cleanly | — |
| `pipeline/test_breaking_episode_id.py` | episode identity as the consumer sees it (ISSUE_65): one story keeps one id across opener/hold/dip/re-trigger, a hold-band pass carries the id although `is_breaking` is false, `breaking_episode_start` marks the opening pass only, a row outside any episode carries none, a new story after the gap gets a new id, a fanned analysis (ETHUSD/ETHEUR) is one episode with one registry row while same-base/different-query FX symbols are not, the id survives a restart whose seed window clipped the episode start, an archived envelope without the fields still parses, and the registry row freezes the opener while only `last_seen_at` advances | — |
| `pipeline/test_breaking_episode_rule.py` | the shared Schmitt trigger itself (ISSUE_82): the three decision flags apart (`opened`/`held`/`in_episode`), the episode start frozen across holds, the gap measured from the last **qualifying** pass so a run of dips cannot keep a story alive forever, the boundary inclusive so a cadence landing exactly on the gap does not flip on rounding, keys independent, the config constructors, and the documented escape hatch (exit == confirm restores the pre-ISSUE_82 grouping); plus the episode **key** — same-query symbols share a key, same-base/different-query ones do not (the USD trio that fired in production), and an unconfigured symbol falls back to its base | — |
| `pipeline/test_breaking_story_rule.py` | which episodes are one story (ISSUE_96): two phrasings of one story join while two stories on one symbol stay apart, a story never crosses an analysis unit, the time window stops a recurring theme fusing across months, single-link chains a developing story whose first and last episode are *not* directly similar (measured: 0.49 and 0.57 across the links, 0.21 end to end), and the two cases that decided the construction — boilerplate alone must not join two stories (raw word overlap scored different stories 0.45 against 0.12 for the same one), and two **identical** reasons must still join (unsmoothed IDF gives a document a cosine of 0.000 with itself once every term appears in every document). Every case carries a realistic surrounding window, because IDF can only learn the boilerplate from a corpus and a two-document test measures a degenerate case the report cannot produce | — |
| `observability/reports/test_breaking_timeline_report.py` | the state timeline (ISSUE_82): the measured XRPUSD series renders `####...#.#.#_#` with 8 flips and 1 episode, the three cell states, the exit gate read from the pipeline's own rule, mechanical (`no_data`) rows excluded, symbols that never broke omitted, a symbol filter narrowing output **without** changing the grouping, fanned ETH legs sharing one episode, the width-adaptive render, and the header naming the episode rule per pipeline (a read-time re-derivation that does not say under which rule is not reproducible) | — |
| `observability/reports/test_corpus_text_report.py` | corpus text (ISSUE_112), DB-backed and seeded through the real `PgVectorStore.upsert` so the columns under test are written by production code: the census splits by treatment and counts surviving carriers; **a stamped row that still carries markup is flagged** (the failure state — a census that can only render the happy path proves nothing, so the ⚠ line and the ✓ line are asserted against each other); an unstamped row is an absence, never a treatment failure; removal measured within the row against its own kept original, and zero on a corpus that arrived clean; a keyword living only inside markup is a phantom hit while the same keyword in prose is not; the gate decides `self_flags`, so a phantom below it is listed without being counted as HIGH-raising; a feed no configured set claims is named rather than dropped; "checked and none found" renders differently from "nothing ran"; the window narrows the flow half and not the census; and both empty branches — a missing corpus table and an empty corpus — render instead of raising, which is the lesson `breaking_report`'s `UnboundLocalError` taught one file over | — |
| `observability/reports/test_prompt_drift_report.py` | prompt drift (ISSUE_110): two versions as separate chronological rows with their own shares; the hold/break ratio separating a *collapse* from a *calm model* on two series with an identical confirm share; mechanical (`no_data`) passes excluded from the distribution, so a frozen corpus cannot read as a calmer prompt; the confirm count taken from the recorded verdict rather than re-derived; a confirm band resting on one analysis unit flagged; a unit counted by its episode key but displayed by its tickers (`ETHUSD/ETHEUR`, and FX queries not leaking into the column); **pooled reads flat while all four split rows move by 9x** — and the report proven to hold no cross-pipeline field; two prompt hashes or two eval models under one version flagged, not split; the >12-value binning fallback folding every version onto one grid; a pipeline header that never names a model its rows contradict; and **parity with `breaking_timeline`** on `scored`/confirm/`mechanical`, so the two aggregations cannot drift apart (the ISSUE_82 lesson). ISSUE_112 added the **setup** half of the grouping key: one `prompt_version` under two `config_fingerprint` values is two rows (the production case — `afe4ac5a3331` and `9458492ce234` both ran under v4), with the counter-example asserting that the same passes pooled into one setup would have reported a single healthy-looking share; the version split is proven not to regress; and an envelope without a fingerprint gets its own `(none)` row rather than being folded into whichever setup ran next | — |
| `observability/reports/test_source_health.py` | host normalization, warn/error split, report format, orphan notice, feed-doctor classifier; ISSUE_107 added the delivery barrier — `EMPTY` (a 2xx carrying nothing) and `STALE` (parses, stopped publishing) against a declared or default age, the doctor's census and its state-present-only legend, and the store-backed `SILENT` rule with the two cases a verdict must not get wrong: it never stacks on a feed that already has a reason, and an unreadable corpus reports NOT APPLIED rather than flagging the whole fleet | — |
| `observability/test_source_poll_log.py` | the diagnostic journal (ISSUE_76): a successful poll round-trips with its duration, **a failed one does too** — the regression the issue exists for, since `StageTimer` keeps nothing for a stage that raises — plus retention pruning only past the window, the once-per-UTC-day prune trigger, and a broken journal that never fails the pass | PostgreSQL |
| `observability/reports/test_source_latency_report.py` | latency + gap aggregation (ISSUE_76): failures never pollute the success percentiles, the `timeout` vs `refused` verdict separates a slow feed from a dead one (the question 2026-08-15 could not answer), the ⚠ fires only relative to the *configured* deadline and needs a real tail rather than one outlier, a gap beyond a feed's own cadence becomes an outage with a polls-missed cost while jitter stays quiet, and a pre-migration database answers empty instead of crashing | PostgreSQL |
| `observability/reports/test_floor_profile_report.py` | the retrieval floor profile (ISSUE_55 groundwork), built from row shapes so the verdicts are covered without a database: `starved` fires only when the nearest article is beyond the floor and `indiscriminate` only when the window median is inside it (both threshold-free), an empty window produces no verdict rather than a false one, and the knee picks the widest gap **below the median** — the bounded case is the load-bearing one, since an unbounded search was measured putting `Litecoin LTC`'s knee at 0.885, above its own median, on a gap out in the sparse tail | — |
| `observability/reports/test_source_contribution_report.py` | the per-feed contribution walk (ISSUE_82 #9): an article cited by many symbols across many passes counts **once** (otherwise a feed's rank becomes a function of pass cadence), breaking citations are tracked separately, a pre-ISSUE_2 source ref without an `article_id` is skipped rather than crashing the walk, the high-volume/low-citation marker needs both halves so a tiny feed is never flagged for being small, and a disabled feed keeps its row | — |
| `observability/test_source_health_store.py` | poll counters, flag+quarantine threshold, recovery reset, event cap, restart-survives quarantine. Plus the ISSUE_84 policy, mirroring `observability/test_budget_guard.py`'s suspend/cool-off/probe/resume shape: the failure type **and its duration** pick the starting rung (table-driven — the `UNREACHABLE` fast/slow split is the load-bearing case, and a one-rung ladder clamps), the ladder escalates 1h→6h→24h across episodes, a bare-integer config still behaves as a single rung, a 403 lands on the top rung on its first episode, cool-off expiry hands out exactly one probe, a successful probe closes the episode as `probe_ok` while a failed one escalates a rung instead of repeating, a fleet-wide failure quarantines nobody and advances no rung (one `correlated` row + a set back-off) while a single failing feed among healthy peers is still quarantined, a thin pass cannot look correlated, returning connectivity closes the event and flags only the feeds that stayed dead, and a pass that dies mid-way still leaves its counters written | PostgreSQL |
| `observability/reports/test_source_quarantine_report.py` | the history surfaces (ISSUE_84), built from row shapes so the arithmetic and wording are covered without a database: an episode priced in the feed's own polls, a correlated event never counted against the policy, the summary's **`to policy` vs `to the outage`** split, 0-based rungs rendered 1-based, a running episode accruing cost, empty and pre-migration answers, and the episode view falling back to the frozen timeline once the journal window has passed | — |
| `observability/test_logging_setup.py` | console + daily-rotating file, idempotent reconfigure, quiet loggers | — |
| `observability/test_budget_guard.py` | cost circuit-breaker: quota-vs-rate-limit, suspend/cool-off/single-probe/resume, soft-daily warn, paid-seam reaction (fake clients) | — |
| `api/test_latest_endpoint.py` | `/latest` serves from store (no run), cold miss, broken-store degrade, catch-all persist; an API-driven pass is stamped `external`, never `scheduled` (ISSUE_87) | — |
| `llm/test_model_catalog.py` | staged model check (ingest + llm), endpoint split, soft-boot warnings | — |
| `pipeline/test_model_governance.py` | pipeline-declared model (required), allowlist gate at assembly | — |
| `llm/test_provider_factory.py` | `llm.provider` → implementation resolution; unknown name fails | — |
| `observability/test_cost_recorder.py` | USD derivation, billing rows, latency column, session accumulators; per-pass scoping (ISSUE_74): a scope collects only its own calls, recording outside one still works, and **two concurrent passes in real threads do not cross-attribute** — the guarantee the removed global lock used to provide, and what makes every envelope's `cost_usd` trustworthy; the pass reason (ISSUE_87) lands on **every** row of its pass — the query embeddings too — stays NULL outside a scope, and does not leak between two overlapping passes | PostgreSQL |
| `observability/reports/test_detection_sweep_report.py` | detection sweep (ISSUE_106) — the reading of the grid, which is what the report exists to protect: an inert live configuration is named **inert, not strict**; a single-feed neighbourhood is called out as duplication while a three-outlet one is not; the three columns are never allowed to pass as one measure; a **thin sample** (under 72h) refuses to read as a finding; the sample reports which `text_normalizer` treatment it measured, because two articles sharing a feed's markup are similar because of the markup; and the story measure is **tested, not assumed** — it separates three outlets on one event from a template series with per-item bodies, and provably cannot when the body is byte-identical | — |
| `observability/reports/test_cost_report.py` / `observability/reports/test_perf_report.py` | section aggregation + pattern tables; fresh/legacy-DB guards. ISSUE_67 groundwork dates the **price basis** on every cost render: the age in days (singular and plural), an absent date saying `NO verification date recorded` rather than passing for current, no staleness verdict at any age — and the seam that had actually broken, `build_cost_report` accepting `prices_checked` and dropping it, invisible to every test that constructed `CostReport` directly | PostgreSQL |
| `contracts/test_typing_contract.py` | the typing convention (CLAUDE.md "Fully typed") over `finiexragengine/`: an AST sweep asserting every parameter and return carries an annotation, plus a `get_type_hints` pass asserting every annotation **resolves** — since PEP 649 made annotations lazy, a name that was never imported no longer fails at import time | — |
| `contracts/test_suite_layout.py` | the suite's own layout ("Layout" above): test basenames stay **unique across the tree** — with no `__init__.py`, pytest imports every module by its bare basename, so two same-named files in different folders collide at collection — plus no `__init__.py` creeping in, nothing landing back at the flat root, and no module *hiding from collection* — a file that lost its `test_` prefix in a move drops out of pytest's `python_files` pattern and out of the three checks above at the same time, so that one looks at every `.py` under `tests/` | — |
| `contracts/test_vocabulary_boundary.py` | ISSUE_94: an unknown `signal`/`basis`/`status`/`data_origin` loads and round-trips, a 72 h window containing one still seeds completely (`get_since`), a typo still fails where a row is written (`hold_result`), the domains stay enumerable | PostgreSQL (one test) |
| `generator/test_mock_signal_generator.py` | ISSUE_93: a generated week loads through the production models, every line names its trigger, `seq` gapless and in commit order, the three evidence invariants on every row, the seq/evidence inversion is present, the archive line declares its time base, and a bare `--rotate daily` puts `data/<stream_id>/<bucket>.jsonl` at the root with stream directories and nothing else beside them (run with `cwd=tmp_path` so the *default* `--out` is what is asserted — the layout a consumer's range read binds to, and the one whose recorded invocation drifted from the shipped files once). Runs the generator as a subprocess | — |
| `schema/test_migration_runner.py` | ordered apply + record, re-run no-op, column added to a populated table, failed migration rolls back whole, checksum drift refuses, duplicate version, boot guard checks-but-never-applies, `-- finiex:no-transaction` (concurrent index needs it / builds with it / one statement only) | PostgreSQL |
| `observability/test_stage_timer.py` / `observability/test_run_footer.py` | shared timing capture + run-metrics footer | — |
| `ui/test_engine_stats.py` / `ui/test_live_display.py` | live dashboard (ISSUE_26): atomic per-stage snapshot swaps, cumulative breaking counters, bounded activity stream, lock-free concurrent writer/reader; rich render on empty/updated stats, healthy sources collapse to `N/N ok` vs a named deviation, activity window; a stalled worker's `last` cell renders red while a healthy one stays neutral (ISSUE_75, asserted on the exported ANSI); the accumulating counters survive concurrent writers (ISSUE_74 — under a forced 1µs GIL switch interval, without which the test would pass with or without the lock); the header names the running version, and omits the segment entirely when none is wired | — |
| `observability/test_resource_gauge.py` | the process gauge (ISSUE_89) and, above all, its two degradation rules: a **missing `psutil` disables it instead of raising** (a deploy is `git pull` on a live host, so a forgotten `pip install` must not block boot), and a **refused socket count costs that one field, not the sample** (Windows denies `net_connections()`, and memory is what the incident was about); plus the ceiling warning firing on the *edge* rather than on every 60s tick, re-arming after a drop, staying silent when unconfigured, and a broken process read being swallowed | — |
| `observability/reports/test_resource_report.py` | the weekly trend (ISSUE_89): min/max/mean; sockets and threads reporting the **last** value rather than a mean (a mean across a restart describes a process that never existed); the delta as the actual signal; `first week` instead of a fabricated delta against zero; `sockets n/a` distinct from `sockets 0` | — |
| `observability/test_resource_sample_store.py` | the sample store (ISSUE_89) against the real migration: round-trip incl. a NULL socket count, the windowed read with and without an upper bound (the weekly needs two adjacent windows, or the delta always reads zero), prune only past the retention window, the once-per-UTC-day trigger keyed on the wall clock rather than the sample's timestamp, and a broken store that never raises | PostgreSQL |
| `observability/test_stall_watchdog.py` | worker liveness (ISSUE_75): the floor beats the factor on a 15s cadence, a slow pass is not a stall, one event per episode (silent for nine days after), exactly one recovery, per-worker independence, a never-run worker is starting not stalled, disabled detects nothing, and a failing alert sink never kills the watchdog | — |
| `rag/test_embedder_cost.py` / `configuration/test_config_override.py` | embed cost wiring · base+user config deep-merge + registry-factory wiring (overrides on every surface) | — |
| `configuration/test_override_report.py` | startup override report: leaf diffs (old → new), id-list paths, `(added)` vs typo flag, once-per-process emit | — |
| `configuration/test_config_fingerprint.py` | input provenance (ISSUE_85) written as a contract: a symbol added/disabled, a feed added/disabled/re-weighted/repointed **in the referenced source set**, retrieval + prompt + model + eval cadence, detection thresholds and the score-defining app leaves all move the fingerprint — while reordering, acquisition pace, timeouts, stream renames and the whole operational app config do not; a `user_configs/` override loaded **through the factories** differs from the tracked file alone (the trap the issue exists for); plus a golden value pinning the canonicalization itself, and a pre-change envelope that still parses; ISSUE_82 added the block-splitting case — `breaking.urgency_threshold` forks the series while `urgency_exit_threshold`/`episode_gap_minutes` do not, the first **dotted** exclusions, pinned by the golden value staying put; ISSUE_107 added the other direction of the feed case — a candidate *declared* with `enabled: false` leaves the fingerprint untouched (the hashed list is `active_sources()`), and flipping it on is the marker | — |
| `observability/test_config_fingerprint_store.py` | the fingerprint registry (ISSUE_85): a setup is stored with the canonical config it stood for, a second boot of the same setup only moves `last_seen` (no false `(new)`), a changed setup lands as a second row diffable against the first, and a broken registry never kills the assembly | PostgreSQL |
| `observability/reports/test_no_data_report.py` | per-symbol no-data aggregation: share, nearest miss vs latest floor snapshot, candidate flag, clean-week render ; ISSUE_5 added the deep tier's contribution — summed over **every** symbol rather than only the silent ones (`rows` is filtered to symbols that went quiet, and the deep tier shows up on the healthy ones), a zero rendered as the finding it is rather than omitted, and envelopes predating the field counting as 0 instead of failing to aggregate| — |
| `observability/reports/test_retrieval_drift_report.py` | retrieval drift (ISSUE_55 groundwork) — the funnel per pipeline, `config_fingerprint` and **weekday**. The central case is that a cell never mixes two weekdays: one fingerprint spanning a weekend is two rows, because pooling Saturday's 10 kept articles with Sunday's 4 reads as 7, which describes neither day and is the exact shape in which a weekend gets attributed to a release. Plus: a delta pairs fingerprints only *inside* one weekday; it runs later-minus-earlier by first appearance rather than by fingerprint name (sorting by name would invert the comparison); `cut%` is a pooled rate and not a mean of means; a pass without a `best_distance` is a missing sample rather than a zero; a pre-deep-tier envelope reads 0; a thin cell is marked and kept, because which fingerprint ran that day is itself evidence; a floor retuned inside a cell is flagged rather than averaged over; a pre-ISSUE_85 envelope groups as `(unstamped)` instead of being dropped; and the `reading` separates *the floor meeting a wider spread* from *the corpus moving away from the query* — the two findings the report exists to tell apart, which only the pair of columns distinguishes | — |
| `observability/reports/test_weekly_report.py` | weekly model: section stitching, STALE derivation + pass census/error taxonomy from the store | PostgreSQL |
| `alerts/test_telegram_client.py` | Bot-API mechanics against a mock transport; the token never leaks into error texts | — |
| `alerts/test_telegram_weekly_format.py` | typed model → HTML sections, section-bound packing ≤ 4096, escaping | — |
| `alerts/test_weekly_scheduler.py` | config→CronTrigger mapping, next_run lifecycle, job failure caught | — |
| `alerts/test_telegram_command_poller.py` | `/report`/`/help` handling, chat restriction, offset advance, backoff + recovery | — |
| `utils/test_archive_layout.py` | archive bucket naming (UTC dates, ISO weeks + year edge), per-stream paths, range→bucket selection — the shared #13 contract | — |
| `utils/test_timeframe.py` | bar-close grid math: frame alignment (M1…D1), strictly-after at an exact boundary, non-UTC normalisation, naive rejection, unknown frame | — |
| `outcome/test_outcome_exporter.py` | DB journal → rotated JSONL: closed-days-only (open bucket skipped), `collected_msc = ts`, per-stream layout, idempotent byte-identical re-export, day filter; incremental scope via the `archive_export_log` flag (skips flagged · writes only newly-closed · `--since`/`--all` ignore but set the flag · open never flagged); weekly auto-export coupling (incremental across runs · off is silent) | PostgreSQL |
| `rag/test_rag_live.py` 💸 | real embeddings end-to-end through store + retriever | `OPENAI_API_KEY` + PostgreSQL, `-m paid` |
| `llm/test_llm_live.py` 💸 | one real structured LLM call (schema + usage) | `OPENAI_API_KEY`, `-m paid` |

### The signal stream (ISSUE_9)

Five suites, split by what they can actually assert.

| Suite | Subject |
|---|---|
| `tests/outcome/test_stream_frames.py` | the wire format itself — named events, the blank line that **dispatches** an event, no `id:` line, compact JSON, the epoch on every frame, and the closed control vocabulary refused at the producing seam |
| `tests/outcome/test_stream_replay.py` | the replay **policy**: the connect snapshot, the window floor, truncation, a cursor ahead of the head, an epoch mismatch. A plan is a value, so every boundary is assertable without a socket |
| `tests/outcome/test_stream_dispatcher.py` | wire order equals `seq` order, the subscribe race (RC-1), the drop policy (RC-6), the sweep that heals a lost notification — plus one end-to-end case over real `LISTEN/NOTIFY` |
| `tests/outcome/test_stream_session.py` | one connection's frame sequence: retry → replay → `live` → frames and keep-alives, the terminal codes, and the cold-stream epoch |
| `tests/api/test_stream_endpoint.py` | what only HTTP decides — the status codes, the parameter refusals, and the two control codes that close the connection |
| `tests/api/test_envelope_range.py` | the range endpoint, and the mapping rule: terminal on the stream ⇒ `409` here, non-terminal marker ⇒ a body field |

**Why the sequence is not driven over HTTP.** An SSE stream never ends, and a test client cannot
close one before the server's response generator finishes — it deadlocks (it did, for two minutes,
which is how `StreamSession` came to be its own unit). An async generator is closed with `aclose()`,
so the sequence is asserted against the generator and the HTTP suite covers only requests that
terminate by themselves.

Two defects these suites caught before anything reached a consumer, recorded because both were
silent by construction: a frame rendered **without the blank line that dispatches an SSE event**
(every frame buffered forever by a conforming client — it looked right in the published sample only
because the generator's join supplied the blank line by accident), and a **cold stream's
`stream_epoch: 0` read as a rewind**, which would have closed every consumer attached to a newly
added pipeline with a false resync signal.

Also extended: `tests/api/test_report_scopes.py`'s walk over identity routes now includes
`/v1/stream/{pipeline_id}`, so the grant on the newest router is checked rather than declared —
and `tests/api/test_pipelines_endpoint.py` pins that `/v1/pipelines` and `/v1/health` report **one**
cadence, from one derivation.

### The `deploy` marker — tests a Linux runner cannot exercise

```bash
pytest -m deploy -q        # ~68 tests, seconds — the platform-sensitive subset
pytest tests/ -q           # everything; the version-bump run (CLAUDE.md) does this on the server
```

The criterion is narrow and deliberate: a dependency the suite **encodes** but this container cannot
**exercise** — an event loop implementation, a console codepage, a socket deadline, a subprocess, a
path separator. It is not "tests that touch the OS" and not "tests that are slow".

It exists because two defects shipped green on 2026-08-28, and neither could have failed here:

- the stream's journal tailer used `psycopg.AsyncConnection`, which needs a selector-based event
  loop. Windows defaults to `ProactorEventLoop`, so on the deployed host every connect raised
  `InterfaceError` and the tail reconnected every two seconds **for 22 hours** — serving connect and
  replay correctly while pushing nothing. The consumer found it by running a real session;
- `experiments/mock_signal_data/generate.py` printed `→` and `·` without calling `use_utf8_output()`,
  so on a non-UTF-8 console it raised `UnicodeEncodeError` and exited non-zero, taking seven tests
  with it. It had never run on Windows at all.

Marked at module level (`pytestmark = pytest.mark.deploy`) with a comment saying which dependency
puts each file in the gate — a marker whose membership rule is not written down becomes a list nobody
can audit. The gate is **not** a substitute for the full run the release rule requires; it is the
fast check for the classes that have actually bitten.

### The stream's platform invariant

`tests/contracts/test_no_async_psycopg.py` asserts that **no module in the package reaches for
psycopg's async API**, because no behavioural test on a Linux runner can. It is paired: the negative
(no `AsyncConnection` anywhere) plus the positive (the sync listener factory, the threaded wait and
the bounded connect are all still present), because absence alone would also be satisfied by deleting
the tail.

Parsed as an **AST rather than grepped** — a textual search trips over the dispatcher's own docstring,
which names `AsyncConnection` precisely in order to explain why it is absent. A guard that fails on
its own reasoning teaches the next person to delete it.

### Two regressions worth naming

`tests/outcome/test_stream_session.py` carries the one that would have caught the outage: it leaves
the dispatcher's tail **unstarted** — the same condition as a listener that cannot open — and asserts
that the keep-alive still reports the *producer's* head. Before the fix the heartbeat read the
dispatcher's cursor, so a stalled tail reported itself as a stalled producer: nearly the right answer
for the wrong reason, and exactly the wrong one when the producer is healthy.

And `tests/generator/test_mock_signal_generator.py` now surfaces the child process's own output on a
non-zero exit. `check=True` names the command and discards the captured stderr, so seven failures
said only "exit status 1" and the cause was guessed twice — wrongly both times — before anyone could
read it. Captured output that is then thrown away is worse than not capturing it.

## Continuous integration

`.github/workflows/tests.yml` runs the free suite on every pull request and on every
push to `master`, against a pgvector service container — the database-backed tests run
for real in CI. Paid tests never run in CI.
